# Data — never commit raw data
data/

# Virtual environment
venv/

# MLflow tracking
mlruns/
mlartifacts/
mlflow.db
reports/model_comparison.csv
# Environment files
.env

# Model binaries — never commit trained models
models/
*.pkl
*.joblib
*.h5